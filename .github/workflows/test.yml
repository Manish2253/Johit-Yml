name: 'Terraform'
  
on:
  push: 
        

env:

  APP_ID: RITM031323

jobs:

  terraform:
    name: "terraform scan & Plan"
    runs-on: ubuntu-latest


    defaults:
      run:
        shell: bash
        
    steps:
    - name: Checkout
      uses: actions/checkout@v2
